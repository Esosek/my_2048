import 'package:flutter/material.dart';

class SmallCounter extends StatelessWidget {
  const SmallCounter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Small counter');
  }
}
